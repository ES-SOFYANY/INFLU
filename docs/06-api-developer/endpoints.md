# Endpoints

> No business endpoints yet — scaffold only. Each Story Implementer adds endpoints
> for their US following `docs/03-tech-lead/module-design.md`.

System endpoints exposed by the scaffold:

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/docs` | Swagger UI |
| GET | `/api/docs-json` | OpenAPI JSON (live) |
